import { ConstraintPipe } from './constraint.pipe';

describe('ConstraintPipe', () => {
  it('create an instance', () => {
    const pipe = new ConstraintPipe();
    expect(pipe).toBeTruthy();
  });
});
